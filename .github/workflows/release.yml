# Release workflow -- ships DORMANT (every line commented out).
#
# Go libraries are "released" simply by pushing a semver tag (vX.Y.Z): the Go
# module proxy serves it automatically, no registry upload needed. This workflow
# adds a test gate and a GitHub Release on tag. Uncomment when ready to cut v0.1.0.
# For shipping `djangogo` CLI binaries too, add goreleaser (https://goreleaser.com).
#
# name: Release
#
# on:
#   push:
#     tags: ["v*"]
#
# permissions:
#   contents: write
#
# jobs:
#   test:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#       - uses: actions/setup-go@v6
#         with:
#           go-version-file: go.mod
#       - run: go build ./...
#       - run: go vet ./...
#       - run: go test -race ./...
#
#   release:
#     needs: [test]
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v6
#         with:
#           fetch-depth: 0
#       - name: Create GitHub release
#         run: gh release create "${GITHUB_REF_NAME}" --generate-notes
#         env:
#           GH_TOKEN: ${{ github.token }}
